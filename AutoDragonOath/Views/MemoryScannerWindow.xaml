<Window x:Class="AutoDragonOath.Views.MemoryScannerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:AutoDragonOath.Converters"
        mc:Ignorable="d"
        Title="Memory Scanner"
        Height="700"
        Width="900"
        Background="#1E1E1E"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Converters -->
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <!-- Text style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <!-- TextBox style -->
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <!-- ComboBox style -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton
                                Name="ToggleButton"
                                Grid.Column="2"
                                Focusable="false"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                ClickMode="Press"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter
                                        Name="ContentSite"
                                        IsHitTestVisible="False"
                                        Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                        Margin="8,5,0,5"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Left"/>
                                    <Path
                                        Name="Arrow"
                                        Grid.Column="1"
                                        Fill="#CCCCCC"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Data="M 0 0 L 4 4 L 8 0 Z"/>
                                </Grid>
                            </ToggleButton>
                            <Popup
                                Name="Popup"
                                Placement="Bottom"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Slide">
                                <Grid
                                    Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border
                                        x:Name="DropDownBorder"
                                        Background="#2D2D30"
                                        BorderThickness="1"
                                        BorderBrush="#3F3F46"/>
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border
                            Name="Border"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3F3F46"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#0E639C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button style -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1177BB"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3F3F46"/>
                    <Setter Property="Foreground" Value="#808080"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary button style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#3F3F46"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#505050"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Success button style -->
        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#107C10"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0E8A0E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Terminal TextBox style -->
        <Style x:Key="TerminalStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#0C0C0C"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontFamily" Value="Consolas, Courier New"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#1F1F1F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Memory Scanner"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,20"/>

        <!-- Input Panel -->
        <Border Grid.Row="1"
                Background="#252526"
                CornerRadius="5"
                Padding="15"
                Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Base Pointer Selection -->
                <TextBlock Grid.Row="0" Grid.Column="0"
                           Text="Base Pointer:"
                           Style="{StaticResource LabelStyle}"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding BasePointers}"
                          SelectedItem="{Binding SelectedBasePointer}"
                          Style="{StaticResource ComboBoxStyle}"/>

                <!-- Info about base pointer -->
                <TextBlock Grid.Row="0" Grid.Column="3" Grid.ColumnSpan="2"
                           Foreground="#888888"
                           FontSize="11"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap">
                    <Run Text="Select which memory base to scan from"/>
                </TextBlock>

                <!-- From Offset -->
                <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="From Offset:"
                           Style="{StaticResource LabelStyle}"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Text="{Binding FromOffset, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputStyle}"/>

                <!-- To Offset -->
                <TextBlock Grid.Row="2" Grid.Column="3"
                           Text="To Offset:"
                           Style="{StaticResource LabelStyle}"/>
                <TextBox Grid.Row="2" Grid.Column="4"
                         Text="{Binding ToOffset, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputStyle}"/>

                <!-- Step Size -->
                <TextBlock Grid.Row="4" Grid.Column="0"
                           Text="Step Size:"
                           Style="{StaticResource LabelStyle}"/>
                <TextBox Grid.Row="4" Grid.Column="1"
                         Text="{Binding StepSize, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputStyle}"/>

                <!-- Data Type -->
                <TextBlock Grid.Row="4" Grid.Column="3"
                           Text="Data Type:"
                           Style="{StaticResource LabelStyle}"/>
                <ComboBox Grid.Row="4" Grid.Column="4"
                          ItemsSource="{Binding DataTypes}"
                          SelectedItem="{Binding SelectedType}"
                          Style="{StaticResource ComboBoxStyle}"/>

                <!-- Info Text -->
                <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="5"
                           Foreground="#888888"
                           FontSize="11"
                           TextWrapping="Wrap">
                    <Run Text="Example: From=0, To=200, Step=4 will scan BASE+0, BASE+4, BASE+8, ... BASE+200. Common steps: 4 (INT/FLOAT), 1 (STRING)"/>
                </TextBlock>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,15">
            <Button Content="Scan Memory"
                    Command="{Binding ScanCommand}"
                    Style="{StaticResource ButtonStyle}"
                    Margin="0,0,10,0"/>
            <Button Content="Scan Common Addresses"
                    Command="{Binding ScanCommonAddressesCommand}"
                    Style="{StaticResource SuccessButtonStyle}"
                    Margin="0,0,10,0"/>
            <Button Content="Clear Output"
                    Command="{Binding ClearCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"/>
            <TextBlock Text="Scanning..."
                       Foreground="#FFA500"
                       FontWeight="Bold"
                       FontSize="13"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"
                       Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Terminal Output -->
        <Border Grid.Row="3"
                Background="#0C0C0C"
                BorderBrush="#1F1F1F"
                BorderThickness="1"
                CornerRadius="3">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto">
                <TextBox Text="{Binding OutputText, Mode=OneWay}"
                         Style="{StaticResource TerminalStyle}"
                         VerticalAlignment="Stretch"
                         HorizontalAlignment="Stretch"/>
            </ScrollViewer>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="4"
                Background="#252526"
                CornerRadius="3"
                Padding="10"
                Margin="0,15,0,0">
            <TextBlock Foreground="#888888"
                       FontSize="11">
                <Run Text="Ready. Use 'Scan Common Addresses' to test known game pointers or 'Scan Memory' for custom range."/>
            </TextBlock>
        </Border>
    </Grid>
</Window>
